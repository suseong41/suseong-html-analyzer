# HTML Scanner — 설계 논의 기록

> 목표: HTML을 파싱해 **XSS 취약점**과 **링크·리소스**를 찾아내는 보안 스캐너 (Go)

---

## 1. 파서를 어디까지 만들 것인가

**논의** — DOM 파서로 만들면 되는가?

**결론** — DOM은 대안이 아니라 **상위 계층**이다. 순서의 문제였음.

```
바이트 → ① Tokenizer → ② Tree Construction → DOM
         (구현 완료)      (보류)              (보류)
```

| 근거 | |
|---|---|
| 필요 충분 | 목표(XSS·링크·리소스)는 "태그 하나"만 봐도 판정 가능 |
| 낭비 없음 | DOM을 만들더라도 ①은 어차피 선행 필수 |
| 난이도 | ②에는 암묵적 태그 닫기, foster parenting 등 함정이 집중돼 있음 |

**재검토 조건** — "`<form>` 안에 CSRF 토큰이 있는가" 같은 **구조 질의**가 요구될 때.

---

## 2. 관통하는 원칙: Parser Differential

> **내 토크나이저가 브라우저와 다르게 해석하는 지점 = 취약점을 놓치는 지점**

일반 파서는 "잘 파싱하면" 성공이지만, 보안 스캐너는 **브라우저와 동일하게** 해석해야 한다.

**따름정리 1** — 정규화는 토크나이저에서 끝낸다. 규칙 쪽에서 대소문자·공백을 매번 신경 쓰게 만들면 언젠가 한 곳을 빠뜨린다.

**따름정리 2** — 판정은 디코딩된 값으로, 리포트 표시는 원본으로. 난독화 자체가 신호이므로 원본을 버리면 안 된다.

---

## 3. 언어 선택: C++ → Go

**논의** — 기존 C++ 프로젝트(`old_c_files`, 1,406줄)를 이어갈 것인가.

**결론** — **Go로 전환.** C++ 자산은 코드가 아니라 **지식**(위협 분류, IOC)만 이식한다.

| 근거 | |
|---|---|
| 퍼징 | `go test -fuzz` 로 30초에 2,070만 케이스. 파서에 필수인데 C++은 libFuzzer+ASan 별도 구성이 필요 |
| 메모리 안전 | 적대적 입력을 파싱하는 도구의 파서 버그 = 스캐너 자체가 RCE 벡터 |
| 크로스 플랫폼 | C++ 버전은 `main.cpp`의 `#else` 중괄호 누락으로 macOS/Linux에서 실행 불가였음 |
| 생태계 | Trivy, Nuclei, gitleaks, Syft/Grype 등 현대 스캐너가 Go로 수렴 |

**뒤집힐 조건** — 기존 C++ 제품에 in-process 라이브러리로 임베딩해야 하는 경우. 해당 없음.

### C++ 버전에서 발견한 결함 (이식 시 수정 대상)

| | 내용 |
|---|---|
| 🔴 | `main.cpp` `#else` 분기에 중괄호 없음 → 비Windows에서 동작 불가 |
| 🔴 | H103의 form 열림/닫힘 반전 → 규칙이 절대 발동하지 않음 |
| 🟠 | meta `content` 대입이 else-if 밖 → 마지막 속성 값으로 덮어써짐 |
| 🟠 | `<!` 처리가 주석 끝까지 건너뛰지 않음 → 주석 안의 코드가 실제 태그로 파싱 |
| 🟡 | `isalpha(char)` — UTF-8 다바이트가 음수라 미정의 동작 |
| 🟡 | `svg:script` 를 RAWTEXT 취급 — 브라우저와 불일치 (실효 없음) |
| ⚪️ | `HtmlLoader.cpp` 전체가 죽은 코드, `cppcore` 부재로 저장소 단독 빌드 불가 |

---

## 4. 구조

| 항목 | 결정 | 근거 |
|---|---|---|
| 패키지 | `tokenizer/` + `scanner/` 분리 | Go의 패키지 단위는 파일이 아니라 디렉터리 |
| 인터페이스 | `Next()` **풀(pull) 모델** | C++의 push(SAX)는 분석기가 파서 문맥을 흉내내야 함 |
| 버퍼 | `[]byte` | 구조 문자가 전부 ASCII, UTF-8은 그대로 통과 |
| 테스트 | 테이블 주도 + `t.Run` | 파서는 케이스가 수십 개로 늘어나는 종류의 코드 |
| `main` | 파일 읽기 + 스캔 호출 + 출력만 | 규칙을 소유하면 테스트할 수 없다 |

### Push vs Pull

C++은 `parser.SetHandler(&analyzer)` 방식이라 분석기가 `m_inForm` 같은 상태를 직접 관리해야 했고, 그것이 H103 반전 버그의 배경이 되었다. Pull 모델에서는 소비자가 루프를 소유하므로 문맥 추적이 자유롭다.

Push의 정당한 장점은 스트리밍이지만, C++ `main`이 파일을 통째로 읽어 넘기고 있어 실현되지 않았다.

---

## 5. 파싱 규칙

| # | 논의 | 결정 |
|---|---|---|
| 1 | `<` 는 항상 태그인가 | **아니다.** 다음 글자가 영문자·`/`·`!`·`?` 일 때만 |
| 2 | 태그 이름 대소문자 | **ASCII 소문자로 정규화** (`<ScRiPt>` 우회 차단) |
| 3 | `strings.ToLower` | **불가.** 유니코드를 건드려(켈빈 기호, 터키어 İ) 브라우저와 어긋남 |
| 4 | `<script>` 내부 | **원시(raw) 모드.** `style`, `textarea`, `title`, `iframe` 등 8종 |
| 5 | RAWTEXT vs RCDATA | `title`/`textarea` 만 문자 참조를 해석 (`Token.Raw`) |
| 6 | 속성 값 | 따옴표 종류(`Quote`)까지 보존 |
| 7 | 중복 속성 | 슬라이스로 **전부 보존**, 조회 시 **첫 번째** (브라우저 동작) |
| 8 | 위치 정보 | 토큰엔 **오프셋만**, 줄:칸은 조회 시 계산 |
| 9 | self-closing | 플래그만 노출. "실제로 닫혔는가"는 트리 구성의 일 |
| 10 | 문자 참조 | **별도 레이어**(`entity.go`). 상태 기계에 섞지 않음 |
| 11 | 이름 참조 세미콜론 | **필수** (숫자 참조는 선택) — 쿼리 스트링 `&copy=2` 보호 |
| 12 | 주석 | `-->` 와 `--!>` 로 닫힘. `Data` 는 내용만 |
| 13 | `</>` / `</ b>` | 각각 **무시** / **주석** (브라우저 동작) |
| 14 | script 내부 `<!--` | escaped → doubleEscaped 상태 추적. **doubleEscaped에서는 `</script>` 가 닫지 않음** |

### 핵심 결정의 근거

**#8 위치 정보** — `t.pos++` 가 14곳이라 증분 갱신은 한 곳만 빠뜨려도 조용히 어긋난다. 오프셋은 이미 `start` 변수로 갖고 있으므로 저장이 공짜고, 계산은 `sort.Search` 이진 탐색 한 곳으로 모인다. 칸(col)은 바이트가 아니라 **글자(rune)** 기준 — 한글 페이지에서 에디터와 어긋나면 리포트가 쓸모없어진다.

**#10 별도 레이어** — 명세는 상태 6개로 다루지만, 상태 기계가 이미 충분히 복잡하고 무한 루프 위험이 커진다. 분리하면 `Unescape("&#106;")` 만 독립적으로 테스트할 수 있다.

**#14** — 양방향으로 틀린다. 존재하지 않는 `<img onerror>` 를 만들어내는 **오탐**이자, 진짜 스크립트 내용을 HTML로 착각해 스크립트 대상 규칙이 안 도는 **미탐**.

---

## 6. 규칙 계층 설계

**논의** — 규칙을 어떤 모양으로 둘 것인가.

**1차 결론** — 순수 함수. `func(tok Token) []Finding`

```
scanner.go   엔진 — 타입, 순회, 오프셋→줄:칸 변환   (거의 고정)
rules.go     규칙 — 무엇을 위험으로 볼 것인가        (계속 자람)
```

**2차 결론(수정)** — H103(Cross-Origin Form POST)을 이식하려니 **"지금 어느 form 안인가"** 를 기억해야 했다. 순수 함수로는 표현 불가.

```go
type Rule interface {
    Check(ctx *Context, tok tokenizer.Token) []Finding
}
type finisher interface {          // 선택적 인터페이스
    Finish(ctx *Context) []Finding
}
type ruleFunc func(...) []Finding  // 무상태 규칙용 어댑터
```

| 결정 | 근거 |
|---|---|
| 상태를 **규칙 안에** 가둔다 | C++은 분석기 전체가 공유해 추적이 안 됐고, 그래서 반전 버그가 남았다 |
| `var rules` → `newRules()` | 상태를 가진 규칙은 스캔마다 새로 만들어야 한다 |
| 모든 토큰을 규칙에 전달 | `</form>` 과 `TextToken` 을 봐야 하는 규칙이 생겼다 |
| `finisher` 를 별도 인터페이스로 | 피싱 페이지는 `</form>` 을 안 닫는 일이 흔하다 |
| `Context.Domain` | "이 파일이 어느 도메인의 것인가"는 파일 안에 없다 |

**`domainOf`의 `@` 처리** — `https://bank.com@evil.com/` 의 실제 접속지는 `evil.com`. 피싱의 고전적 수법이라 반드시 마지막 `@` 뒤를 취해야 한다.

---

## 7. 구조 질의 — 열린 요소 스택

**논의** — 규칙이 `<form>` 문맥을 `inForm` 플래그로 들고 있었다. 충분한가?

**결론** — 아니다. **브라우저와 어긋나는 미탐이 실재했다.**

```html
<form action="https://evil.com/steal">
  <form action="/safe">            <!-- 명세: form pointer 가 null 이 아니면 이 토큰은 무시된다 -->
    <input type="password">        <!-- 실제로는 바깥 evil.com 폼에 속한다 -->
  </form>
</form>
```

플래그 방식은 안쪽 `action="/safe"` 로 덮어써 **상대 경로로 판정하고 조용히 통과**시켰다.

**전체 DOM 트리 대신 열린 요소 스택**을 만들었다.

| 질문 | 플래그 | 스택 | 트리 |
|---|:---:|:---:|:---:|
| "지금 form 안인가" | ⚠️ | ✅ | ✅ |
| 중첩 form (form element pointer) | ❌ | ✅ | ✅ |
| "이 텍스트가 script 안인가" | ❌ | ✅ | ✅ |
| `<input form="id">` 원격 연결 | ❌ | ❌ | ✅ |
| CSS 셀렉터 질의 · foster parenting | ❌ | ❌ | ✅ |

| 결정 | 근거 |
|---|---|
| `scanner/stack.go` 에 둔다 | 토크나이저는 ①단계, 스택은 ②단계. 계층을 섞으면 토크나이저를 재사용할 수 없다 |
| void 요소는 쌓지 않는다 | `<img>`, `<input>` 등 14종은 내용을 가질 수 없다 |
| `closesP` · `autoClose` 표 | `<ul><li>a<li>b` 를 형제로 만들지 않으면 조상 질의가 전부 틀어진다 |
| `SelfClosing` 을 **무시**한다 | HTML에서 `<div/>` 는 닫히지 않는다. 8교시에 미뤄둔 판단을 여기서 내렸다 |
| 스택 갱신을 규칙보다 **먼저** | `<input>` 을 볼 때 이미 `<form>` 이 스택에 있어야 한다 |

**부수 효과** — 문맥을 스택이 흡수하자 규칙에서 상태가 사라졌다. `formOriginRule`(구조체 4필드 · 55줄)이 순수 함수 30줄이 되었고, 발견 위치도 `</form>` 이 아니라 문제의 `<input>` 을 가리키게 됐다.

---

## 8. 도구화

### 8.1 CLI

`old_c_files/main.cpp` 의 `<htmlfile> [url]` 인터페이스를 가져오고 Go답게 두 가지를 더했다.

| 결정 | 근거 |
|---|---|
| `main()` 은 `run()` 을 부르기만 | `os.Exit` 는 `defer` 를 실행하지 않는다 |
| 출력이 `파일:줄:칸:` 형식 | 컴파일러·린터의 표준. 에디터가 클릭으로 점프한다 |
| 발견은 stdout, 요약은 stderr | 기계가 읽을 것과 사람이 볼 것을 분리 |
| 종료 코드 `0`/`1`/`2` | 발견 없음 / 발견 있음 / 오류. **`1`과 `2`를 구분**해야 CI에서 쓸 수 있다 |

### 8.2 퍼징

무작위 입력의 정답은 알 수 없으므로, 결과 대신 **항상 참이어야 하는 성질**을 검사한다.

| 대상 | 검사하는 성질 |
|---|---|
| `FuzzTokenizer` | panic 없음 · 토큰 수 ≤ 입력 길이(무한 루프) · 오프셋이 범위 안 |
| `FuzzUnescape` | 출력이 입력보다 **길어질 수 없다** |
| `FuzzScan` | 발견의 위치가 유효 · **두 번 스캔하면 같은 결과** |

`FuzzScan` 의 마지막 항목은 6절의 `newRules()` 결정을 코드로 못 박은 것이다. 누군가 전역 `var rules` 로 되돌리면 즉시 실패한다.

**씨앗이 곧 탐색 지도다.** `f.Add` 로 넣은 11개는 각 상태(원시 모드·escaped·주석·문자 참조·bogus comment·UTF-8)의 대표다. `<script><!--<script>` 를 무작위로 만들어낼 확률은 사실상 0이므로, 씨앗이 없으면 그 경로는 영원히 탐색되지 않는다.

**상설화의 핵심** — 크래시를 찾으면 그 입력이 `testdata/fuzz/<함수명>/<해시>` 로 저장된다. 이 파일을 커밋하는 순간 `-fuzz` 없이 도는 **영구 회귀 테스트**가 된다.

### 8.3 CI

| 잡 | 언제 | 목적 |
|---|---|---|
| `test` | 푸시·PR | gofmt · vet · test · build · **스모크** |
| `fuzz` | 푸시·PR(30초) + 주간(10분) | 매트릭스로 3대 병렬 |

스모크 단계는 **빌드된 바이너리로 자기 `testdata/` 를 스캔**한다. 정상 페이지는 `exit 0`, 악성 샘플은 `exit 1` 이어야 통과다. 오탐뿐 아니라 **미탐도 막는다** — 규칙이 망가지면 악성 샘플에서 아무것도 안 나온다.

러너는 잡마다 새 VM이라 `testdata/fuzz/` 크래시 파일이 잡 종료와 함께 사라진다. `upload-artifact` 로 건져내야 커밋할 수 있다.

---

## 9. 원본 HtmlScanner 분석 — 무엇이 실패했는가

이 프로젝트의 출발점은 `../HtmlScanner`(C++, 13,838줄)였다. 여러 지표를 모아 최종 심각도를 매기려 했으나 오탐·미탐의 바다가 되었고, 뒤엎기를 반복했다. 그 원인을 분해한다.

| | |
|---|---:|
| 소스 | 13,838줄 |
| 탐지 항목 | 9개 (H101~H801 + H000) |
| 산출 지표 | 약 130개 |
| 조정 가능한 임계값 | **36개** |
| 점수 누적 지점 | **105곳** |
| 테스트 | 46개 — **전부 양성** |

### 9.1 점수를 합산해 심각도를 만들었다

```cpp
if (m_ui.headingSparse)              { m_ui.baseScore += 3; }
if (m_ui.thinContent == "very_thin") { m_ui.baseScore += 6; }
if (!m_ui.hasViewport)               { m_ui.baseScore += 2; }
...
if (total_score >= 85) return "HIGH";
if (total_score >= 25) return "WARN";
```

`<meta viewport>` 가 없으면 +2점. 그냥 오래된 페이지인데 위험 점수가 붙는다. 이런 게 105곳이고, **25점이라는 경계에 근거가 없다.** 정상적인 사내 위키 한 페이지가 WARN이 된다.

> 우리 프로젝트에는 점수 합산이 **한 곳도 없다.** 규칙은 각자 독립적으로 발견을 내고 심각도를 스스로 정한다. `cross-origin-password-form` 은 언제나 HIGH이며 다른 신호와 더해지지 않는다.

### 9.2 테스트 46개가 전부 양성

```python
("h101", "missing_meta_tags.html", ["H101"], 1),
("h201", "viewport_missing.html",  ["H201"], 1),
```

전부 "이 악성 샘플에서 이 코드가 나오는가"만 묻는다. **정상 페이지가 단 하나도 없다.**

이 테스트를 100% 통과시키는 가장 쉬운 방법은 **모든 페이지에서 모든 코드를 내보내는 것**이다. 미탐은 0이 되고 오탐은 측정되지 않는다.

"탐지 로직이 정확한지 검증하지 못했다"는 문제의 정체가 이것이다. **양성 테스트는 미탐만 막는다.**

### 9.3 임계값 36개가 서로를 상쇄한다

`Detection.txt` 에 "Safety Caps 조정"이 반복된다. 오탐이 터지면 캡을 씌우고, 미탐이 생기면 가중치를 올린다. 임계값 36개의 상호작용은 조합이 사실상 무한하고, **어떤 값이 옳은지 판정할 방법이 없다.** 뒤엎기를 반복하게 만든 직접 원인이다.

### 9.4 검증 불가능한 지표

`brand_similarity = 0.47` 이 맞는 값인지 누구도 판정할 수 없다. 정답이 없으니 테스트를 쓸 수 없고, 테스트가 없으니 고쳐도 나아졌는지 알 수 없다.

### 9.5 이식의 기준

**"이 규칙이 틀렸다는 것을 테스트로 증명할 수 있는가?"** 하나다.

| 가져온 것 | 버린 것 |
|---|---|
| POST + password + 외부 eTLD+1 | `thin_content`, `word_count`, `heading_sparse` |
| form action IP 리터럴 | `has_viewport` 없음 → +2 |
| meta refresh → `data:` URI | `brand_similarity` (LCS 유사도) |
| `<base href>` 호스트 불일치 | `stale_year` (푸터 연도 노후) |
| iframe sandbox 토큰 남용 | `free_email_count` |
| 실행 가능한 `data:` URI | **총점 합산 → 심각도 라벨** |
| 웹셸 시그니처 · 유출 채널 · 제로폭 | H801 MITRE 자동 분류 (신뢰도 0.0~1.0) |

### 9.6 측정이 규칙을 먼저 걸러낸다

규칙을 쓰기 전에 **정상 페이지에서 몇 건 나오는지** 잰다.

| 원본 규칙 | 실측 | 판정 |
|---|---|---|
| iframe `sandbox` 누락 | 1건 — Google Tag Manager 정상 iframe | **버림** |
| 스킴리스 `//` URL | 2건 — `//portal.jnu.ac.kr/...` 정상 | **버림** |
| `data:` base64 길이 ≥ 100 | 0건, 그러나 길이는 위험과 무관 | **재설계** |
| form action 미지정 | 현재 URL로 제출 — 정상 동작 | **버림** |

세 번째가 특히 중요하다. `<img src="data:image/png;…10KB…">` 는 무해하고 `<iframe src="data:text/html,…">` 는 50자여도 위험하다. **길이가 아니라 "어떤 타입을 어디에 넣었는가"** 가 위험을 결정한다.

### 9.7 규칙을 추가하는 절차

1. 판정 기준을 **한 문장으로 쓴다** — 못 쓰면 그 규칙은 버린다
2. **음성 케이스를 먼저 쓴다** — "정상 페이지에서 안 나오는가"
3. 양성 케이스를 쓴다
4. 구현한다
5. **`testdata/jnu_main.html` 탐지 수가 늘지 않는지 확인한다** — 늘면 오탐이다

5번이 원본에 없던 안전장치다. 실제 정상 페이지를 회귀 기준으로 삼는다.

---

## 10. 분석의 한계는 발견이 아니다

**논의** — SPA 셸처럼 내용을 스크립트가 그리는 페이지를 어떻게 다룰 것인가.

원본의 답:

```cpp
m_ui.baseScore += 12;   // SPA 셸 가중치
```

**SPA라는 사실이 위험 점수가 됐다.** 정상 React·Vue 사이트가 전부 +12점을 받는다.

**우리의 답** — SPA는 위험하지 않다. **우리가 그 내용을 못 볼 뿐이다.** 둘은 완전히 다른 진술이고 리포트에서 다른 자리를 차지해야 한다.

| | 의미 | 종료 코드 |
|---|---|---|
| `Finding` | 이 페이지에 이런 위험이 있다 | 영향 있음 (exit 1) |
| `Notes` | 우리가 이 페이지를 다 보지 못했다 | **영향 없음** |

| 결정 | 근거 |
|---|---|
| `Notes` 는 `[]string` | `Severity`·`Line` 을 주면 "위험도가 있는 무언가"가 되어버린다 |
| 판정은 `visibleText == 0` | 임계값을 쓰지 않는다. `< 100` 으로 잡으면 짧은 랜딩 페이지가 전부 걸린다 |
| `title`·`noscript`·`textarea`·raw 제외 | 특히 `noscript` — 거의 모든 SPA 셸에 있어서, 안 빼면 전부 놓친다 |
| 종료 코드에 넣지 않는다 | SPA 스캔이 exit 1 이면 정상 React 앱마다 CI가 깨진다 |

`<div id="app">Loading...</div>` 처럼 짧은 텍스트가 있는 셸은 **의도적으로 놓친다.** 과소 보고가 잘못된 보고보다 낫다.

---

## 11. 규칙 코드 체계 — H번호를 쓰지 않는 이유

**논의** — 원본들처럼 H번호를 매길 것인가.

**결론** — 아니다. 두 원본이 **같은 번호를 다른 뜻으로** 쓴다.

| 코드 | `old_c_files` | `HtmlScanner` |
|---|---|---|
| H101 | Phishing API Abuse | **Head** anomalies |
| H301 | 익스플로잇킷 대역 | **Form** handling |
| H601 | 마이너 대역 | **External link** anomalies |

`old_c_files` 는 위협 종류별, `HtmlScanner` 는 HTML 위치별이다. **연속성을 줄 단일 원본이 없다.**

번호 자체의 문제도 있다.

| 문제 | |
|---|---|
| 읽을 수 없다 | `H304` 를 보고 무슨 규칙인지 알 수 없다 |
| 재배치 압력 | 대역이 차면 재번호 |
| 레지스트리 표류 | `Detection.txt`(7.8KB)처럼 코드와 별개인 문서를 손으로 맞춰야 한다 |

### 세 축을 분리한다

> **`Code` 는 신원, `Class` 는 종류, `Severity` 는 정도.**
> 셋을 한 값에 욱여넣으면 반드시 충돌한다.

원본은 번호 하나에 종류와 위치를 함께 담으려다 실패했다. `exfil-channel` 은 `<form action>` 과 스크립트 본문 **양쪽에서** 나오는데, 위치 기반 체계에서는 배치할 수 없다.

```
exfiltration / execution / origin / supply-chain / evasion / hardening
```

애매한 경우 **"이 규칙이 잡는 구별되는 신호는 무엇인가"** 를 묻는다.

- `obfuscated-eval` → Execution 이 아니라 **Evasion**. `eval()` 단독은 보고하지 않기로 했으므로, 구별되는 신호는 난독화 자체다.
- `iframe-sandbox-escape` → **Origin**. 탈출의 결과는 프레임 콘텐츠가 동일 출처 권한을 얻는 것이다.

---

## 12. 측정 결과

`testdata/jnu_main.html` (163KB, 실제 페이지) 기준.

### 12.1 원시 모드 도입 — 오탐 127건 제거

`<script>` 내부 JS 문자열(`$(...).append("<a class='...'>")`)이 가짜 태그로 잡히고 있었다.

| | 이전 | 이후 | 차이 |
|---|---:|---:|---:|
| 시작 태그 총계 | 831 | **704** | −127 |
| div | 241 | 200 | −41 |
| a | 132 | 106 | −26 |
| img | 58 | 44 | −14 |
| option | 22 | 12 | −10 |

> **숫자가 줄어드는 것이 개선이다.** 오탐이 많으면 결국 아무도 보지 않는다.

### 12.2 속성 파싱의 부수 효과

따옴표 안의 `>` (`alt="연구장비활용<br/> 장비검색"`)에서 태그가 조기 종료되고 있었다. 3곳이 고쳐졌고, 그중 한 `<a>` 태그의 **`target="_blank"` 가 그동안 스캐너에 보이지 않았다.**

### 12.3 현재 탐지 — 8건 (규칙 15종)

```
2468:5    MEDIUM supply-chain  [sri-missing]         www.googletagmanager.com
 740:145  LOW    evasion       [zero-width]          텍스트에 U+200B
1164~1328 LOW    hardening     [inline-handler]      onclick ×5
 123:92   INFO   hardening     [target-blank-no-rel] 80곳을 1건으로 집계
```

**실제 XSS 취약점은 0건.** 규칙이 15종인데 정상 페이지에서 8건만 나오고, 그중 6건이 `hardening`(취약점 아님)이다.

| 분류 | jnu_main (정상) | malicious_sample |
|---|---:|---:|
| exfiltration | — | 2 |
| execution | — | 2 |
| evasion | 1 | 1 |
| supply-chain | 1 | — |
| hardening | **6** | — |

**분류만으로 두 페이지의 성격이 갈린다.** 원본이 총점 하나로 하려던 일이 점수 합산 없이 된다.

### 12.4 리소스 규칙을 구현하며 정정한 것

이전 기록에 "혼합 콘텐츠 6건"이라 적었으나 **오탐이었다.** 그 6건은 전부 `<a href="http://...">` 였다.

> 혼합 콘텐츠는 **브라우저가 자동으로 가져오는 하위 리소스**(script·img·css·iframe)가 평문일 때다. `<a href>` 는 사용자가 클릭해 이동하는 링크라 현재 페이지의 보안 경계를 깨지 않는다.

`isSubresource(tag, attr)` 로 정확히 판별하니 이 페이지의 혼합 콘텐츠는 **0건**이다. 같은 속성 이름이라도 태그에 따라 의미가 다르다 — `href` 는 `<link>` 일 때만 하위 리소스다.

## 13. 탐지 ≠ 취약점

> **탐지** — 패턴이 존재한다
> **취약점** — 공격자가 제어하는 입력이 그 자리에 도달한다

정적 HTML만 보는 스캐너는 후자를 **증명할 수 없다.** 입력의 출처(taint source)를 모르기 때문이다.

현재 인라인 핸들러 5건은 전부 개발자가 하드코딩한 문자열이다. 그래서 이렇게 보고해야 정확하다:

> "인라인 이벤트 핸들러가 5곳 있다. 이 페이지는 `script-src 'unsafe-inline'` 없이는 CSP를 적용할 수 없다."

XSS 취약점이 아니라 **CSP 도입을 가로막는 요소**다. 심각도를 처음부터 타입으로 넣은 이유이기도 하다 — 80건을 빨갛게 띄우면 진짜 문제 5건까지 같이 무시당한다.

---

## 14. 남은 것

| 항목 | 상태 | 비고 |
|---|---|---|
| 전체 이름 참조 표 | 보류 | 2231개. 우회에 실제로 쓰이는 35개는 구현됨 |
| `<input form="id">` 원격 연결 | 미지원 | 위치가 아니라 참조 관계. 스택으로는 원리적으로 불가 |
| 전체 트리 구성 | 미착수 | foster parenting, 삽입 모드 23개 |
| 리소스 IP 리터럴 · 위험 확장자 링크 | 미이식 | `isIPLiteralHost` 재사용 가능 |
| punycode 호스트 | 보류 | 정상 IDN 과 구별하려면 혼합 스크립트 판정이 필요 |
| SPA 렌더링 후 분석 | 범위 밖 | 헤드리스 브라우저 필요. 현재는 `Notes` 로 한계만 보고 |

### 이식 완료 — 원본과 달라진 점

| 항목 | 원본 | 우리 구현 |
|---|---|---|
| 웹셸 시그니처 | H900 | 스크립트 **본문**을 실제로 볼 수 있다. 시그니처 4개로 좁게 유지 |
| 유출 채널 | H101 | `<form action>` + **스크립트 본문**. 요즘 피싱 키트는 `fetch()` 를 쓴다 |
| 제로폭 난독화 | H001 | 속성값만 보던 것을 **텍스트까지**. 원본은 실제 사례를 못 잡는다 |
| Cross-Origin 폼 | H103 | 열림/닫힘 반전 버그 수정 · 중첩 form · `@` userinfo |
| `eval()` 경고 | Log_Warn | 단독은 노이즈라 **디코더와의 조합**일 때만 |
| meta refresh | H102 | `data:`/`javascript:` 만. 외부 도메인 refresh 는 정상이라 제외 |
| iframe sandbox | H201 | **누락은 버리고** 토큰 남용만 |
| `data:` URI | H701 | **길이 임계값을 버리고** 타입 × 컨텍스트로 |
| form action | H301 | IP 리터럴만. 스킴리스·미지정은 정상이라 제외 |

## 15. 반복해서 확인하는 것

- **`pos`가 반드시 전진하는가** — 새 상태를 추가할 때마다 모든 경로를 확인한다. 이제 `FuzzTokenizer` 가 자동으로 검사한다.
- **`go build` → `go vet` → `go test`** 순서 — `go build` 는 `_test.go` 를 컴파일하지 않는다.
- **테스트 실패 시 첫 질문** — "코드가 틀렸나, 기대값이 낡았나?"
- **음성 전부 통과 + 양성 전부 실패** — 규칙이 아예 호출되지 않거나 이름이 안 맞는다는 신호다. 로직 버그면 이렇게 깔끔하게 갈리지 않는다.
- **경과 시간과 실행 수를 먼저 보라** — 30초를 지정했는데 0.3초에 끝났다면 아무것도 안 돈 것이다.

### 도구가 못 잡은 결함들

전부 컴파일러도 `go vet` 도 통과시켰다. **테스트와 CI가 유일한 방어선이었다.**

| 결함 | 증상 | 잡은 것 |
|---|---|---|
| `if t.isTagStart()` 에서 `!` 누락 | 무한 루프 | 실행 |
| `s = s` (자기 대입) | `//cdn.x/` 도메인이 빈 문자열 | 테스트 |
| `finisher` 호출 루프 누락 | 닫히지 않은 `<form>` 을 보고하지 않음 | 테스트 |
| `" atob("` 앞 공백 하나 | **가장 흔한 난독화 형태를 통째로 놓침** | 테스트 |
| `"iframe-snadbox-escape"` 오타 | 규칙이 영원히 0건 | 테스트 |
| `ruleObfuscateEval` 중복 등록 | 발견이 2배 | 테스트 |
| `countCode` 를 제품 코드에 배치 | 테스트 헬퍼가 최종 빌드에 포함 | 리뷰 |
| `urlMixedContent` 죽은 함수 | 미등록 + `https` 를 평문으로 오판 | 리뷰 |
| `.gitignore` 의 `coverage.*` | `scanner/coverage.go` 가 커밋되지 않음 | **CI** |

마지막 항목은 로컬 테스트로 절대 잡히지 않는다. 러너는 git 서버에서 clone 하므로 **커밋되지 않은 파일은 존재하지 않는다.**

- **`.gitignore` 패턴에는 `/` 를 붙인다** — `coverage.*` 는 어느 깊이에서든 잡는다. `/coverage.out` 이어야 루트만 잡는다.
- **큰 변경 뒤에는 퍼징을 길게** — CI의 30초는 회귀 방지용이지 탐색용이 아니다.
